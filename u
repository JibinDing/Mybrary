SELECT
    APPLICATION_IDENTIFIER,
    APPLICATION_NUMBER,
    ACCOUNT_NUMBER,
    ACCOUNT_IDENTIFIER,
    ACTIVE
FROM FDP_UK_CARDS_DB.CREDIT_CARD_ACCOUNT
WHERE ACTIVE = '1'
  AND (
       APPLICATION_IDENTIFIER LIKE '10001090|%'
    OR APPLICATION_IDENTIFIER LIKE '10001031|%'
  )
LIMIT 100;