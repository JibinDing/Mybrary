SELECT
    APPLICATION_NUMBER,
    ACCOUNT_IDENTIFIER,
    LENGTH(APPLICATION_NUMBER) AS app_num_len,
    LENGTH(ACCOUNT_IDENTIFIER) AS acct_id_len
FROM FDP_UK_CARDS_DB.CREDIT_CARD_ACCOUNT
WHERE ACCOUNT_IDENTIFIER IS NOT NULL
LIMIT 20;