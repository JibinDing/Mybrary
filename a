SELECT
    FULL_APPLICATION_STATUS_CODE,
    COUNT(*) AS ROW_COUNT,
    COUNT(DISTINCT APPLICATION_IDENTIFIER) AS APPLICATION_COUNT
FROM BASE_DATA
WHERE LINKED_APPLICATION_NUMBER IS NOT NULL
GROUP BY 1
ORDER BY 2 DESC;