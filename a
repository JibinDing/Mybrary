MAX(
    CASE
        WHEN UPPER(TRIM(CSE.CASE_STATUS_CODE)) = 'COMPLETE' THEN 1
        ELSE 0
    END
) OVER (
    PARTITION BY APP.LINKED_APPLICATION_NUMBER
) AS HAS_COMPLETE


QUALIFY RN = 1
    AND HAS_COMPLETE = 1
    AND UPPER(TRIM(CSE.CASE_STATUS_CODE)) <> 'COMPLETE';