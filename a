SELECT
    APPLICATION_CREATED_DATE,
    LEVEL_1_CHANNEL_NAME,
    LEVEL_2_CHANNEL_NAME,
    SUM(API_ACCEPT_ON_US_CHECK_COUNT) AS AGGREGATOR_2ND_CHECK
FROM APPLICATION_SUMMARY_FINAL
WHERE APPLICATION_CREATED_DATE = '2026-08-01'
GROUP BY 1,2,3
ORDER BY 4 DESC;