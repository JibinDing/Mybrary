WITH BASE AS (
    -- 上面的 analysis query
)

SELECT
    FULL_APPLICATION_STATUS_CODE,
    COUNT(*) AS ROW_COUNT,
    COUNT(DISTINCT APPLICATION_IDENTIFIER) AS APP_COUNT
FROM BASE
GROUP BY 1
ORDER BY 2 DESC;