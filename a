SELECT
    REPORT_HOUR,
    COUNT(*) AS AGGREGATED_ROW_COUNT,
    SUM(MEASURE_VOLUME) AS TRANSACTION_VOLUME,
    SUM(MEASURE_VALUE) AS TRANSACTION_VALUE
FROM BDP_UK_CARDS.FACT_SMARTPAY_TRANSACTION_SUMMARY
WHERE REPORT_HOUR >= '2026-09-09 00:00:00'
GROUP BY REPORT_HOUR
ORDER BY REPORT_HOUR;