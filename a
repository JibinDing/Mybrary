SELECT
    COUNT(*) AS ROW_COUNT,
    COUNT(DISTINCT APPLICATION_IDENTIFIER) AS DISTINCT_APPLICATIONS,
    COUNT_IF(APPLICATION_IDENTIFIER IS NULL) AS NULL_APPLICATION_IDS
FROM FQ_FINAL2
WHERE CASE_CREATION_DATE = '2026-08-01'
  AND APP_FLAG = 'Quote To App';