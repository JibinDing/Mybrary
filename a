SELECT
    APPLICATION_IDENTIFIER,
    APP_FLAG,
    CHANNEL_LEVEL_1,
    CHANNEL_LEVEL_2,
    CHANNEL_LEVEL_3,
    ACQUISITION_SEGMENT2,
    REQUESTED_PRODUCT_DMI_CODE
FROM <OLD_PRE_AGG_CTE>
WHERE APPLICATION_CREATED_DATE = '2026-08-01'
  AND APP_FLAG = 'Quote To App';

SELECT
    APPLICATION_IDENTIFIER,
    APP_FLAG,
    LEVEL_1_CHANNEL_NAME,
    LEVEL_2_CHANNEL_NAME,
    LEVEL_3_CHANNEL_NAME,
    ACQUISITION_PRODUCT_NAME,
    REQUESTED_PRODUCT_DMI_CODE
FROM <NEW_PRE_AGG_CTE>
WHERE APPLICATION_CREATED_DATE = '2026-08-01'
  AND APP_FLAG = 'Quote To App';