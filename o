Thanks. Now please generate the minimum SQL change only.

Do not rewrite the full pipeline.

Please provide:
1. The exact LEFT JOIN to add in STG_2 to map short application id to long application id using application_results_sum_caselg_main.
2. The exact new columns to select in STG_2.
3. The exact LEFT JOINs to add in IHQ_BASE for:
   - FDP_UK_CARDS_DB.CREDIT_CARD_APPLICATION
   - FDP_UK_CARDS_DB.CREDIT_CARD_ACCOUNT
4. The exact new columns to carry forward through:
   IHQ_BASE1, IHQ_COMB, ALL_APPS, FQ_BASE
5. The validation CTE to insert after FQ_BASE and before BASE_SUM1.
6. The final SELECT statement to run validation only.

Important:
- Keep existing logic unchanged.
- Use LEFT JOIN only.
- Do not add new filters that could reduce existing row counts.
- Account number should only be used for validation / booked account analysis.
- Return SQL snippets only, grouped by CTE name.