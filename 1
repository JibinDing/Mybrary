Current implementation follows the existing Cards Aggregator production pattern.

No explicit clustering / partitioning strategy is currently implemented in the reference deployment scripts.

The existing pattern uses logical rolling-window filtering based on timestamp/date columns rather than physical partitioning.