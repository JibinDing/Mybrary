Hi Arun,

As discussed, the SmartPay BDP join logic has now been implemented using TRANSACTION_IDENTIFIER as the joining key between:

- PAYMENT_GATEWAY_AUTHORIZATION_TRANSACTION
- PAYMENT_GATEWAY_TRANSACTION_STATUS

The transformation and aggregation logic are working correctly, however the current DEV sample data does not contain matching TRANSACTION_IDENTIFIER values across both tables.

Would it be possible to load a few matching sample records into the DEV environment (even 1–2 matching transactions would help) so that we can properly validate:

- join behaviour
- transaction status derivations
- end-to-end unit testing
- testing evidence for the production CR

This would help us complete the remaining validation activities for the SmartPay BDP pipeline.

Thanks!