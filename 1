Completed detailed analysis of BDP aggregator SQL logic and derived key reporting fields.

- Broke down core KPI metrics across the full customer journey:
  - API Quote stage (TOTAL_API_QUOTE and outcomes: Accept / Refer / Decline)
  - On-Us Eligibility stage (TOTAL_ON_US_CHECK and outcomes)
  - Application stage (TOTAL_ACCEPT_APPLICATIONS_* and On-Us application metrics)
  - Cross-stage conversion metrics (e.g., TOTAL_API_QUOTE_ACCEPT_CHECK, TOTAL_API_ACCEPTS_US_*)

- Documented derivation logic for key fields including:
  - Source CTEs and dependencies
  - Transformation logic (CASE WHEN conditions, filters, aggregation)
  - Business meaning and mapping to customer journey stages

- Identified and validated the 3-stage funnel structure:
  1. API (Aggregator) eligibility check
  2. On-Us eligibility check
  3. Final application decision

- Started refining derived attributes documentation:
  - Differentiated direct mappings vs derived fields
  - Added simplified business logic explanations for derived metrics

- Cross-referenced dashboard metrics with SQL outputs to ensure alignment

Next Steps:
- Finalise documentation of CTE-level data flow (FQ_BASE → ALL_APPS → BASE_SUM layers)
- Clarify decision logic (QUOTE_DECISION mapping) across stages
- Continue enhancing transformation logic descriptions for derived fields
- Capture open questions for SME (Steve) where logic is ambiguous