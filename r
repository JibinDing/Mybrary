Trace NET_MONTHLY_INCOME_AMOUNT through the SQL.

Return only:
1. Source CTE and exact select line
2. Every later CTE where it is passed
3. Whether it reaches BASE_SUM_FINAL
4. Whether any SELECT * passes it automatically

No explanation.