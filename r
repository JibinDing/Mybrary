Please search this SQL and return only the following:

1. The CTE or SELECT section that contains:
- APPLICATION_ID
- REQUESTED_PRODUCT_CLASS_CODE
- SOURCE_PRODUCT_CODE
- the application date field

Include the source table name and about 10 lines above and below.

2. The section where the CREDIT_CARD_APPLICATION_DECISION_OUTCOME table is joined.

Include:
- the table alias
- the JOIN condition
- whether these fields are selected:
  APR_PERCENTAGE_APPLIED_AMOUNT
  ORIGIN_PART_2
  ORIGIN_PART_3

Keep the response short. Do not explain the SQL. Just copy the relevant code sections and label them Section 1 and Section 2.